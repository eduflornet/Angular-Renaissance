import { ImgBrokerDirective } from './img-broker.directive';

describe('ImgBrokerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgBrokerDirective();
    expect(directive).toBeTruthy();
  });
});
